--[[ 
    Vvv PREMIUM - Edición HITBOX EXPANDER
    Alcance calibrado y colisiones aumentadas.
]]--

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local LocalPlayer = Players.LocalPlayer

if CoreGui:FindFirstChild("VvvHub_Final") then CoreGui.VvvHub_Final:Destroy() end

local ScreenGui = Instance.new("ScreenGui", CoreGui)
ScreenGui.Name = "VvvHub_Final"

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 160, 0, 95)
MainFrame.Position = UDim2.new(0.5, -80, 0.2, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 12)
MainFrame.Active = true
MainFrame.Draggable = true

local UIStroke = Instance.new("UIStroke", MainFrame)
UIStroke.Color = Color3.fromRGB(0, 255, 255)
UIStroke.Thickness = 2
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

local Title = Instance.new("TextLabel", MainFrame)
Title.Text = "Vvv PREMIUM"
Title.Size = UDim2.new(1, 0, 0.4, 0)
Title.TextColor3 = Color3.fromRGB(0, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.BackgroundTransparency = 1
Title.TextSize = 14

local ToggleBtn = Instance.new("TextButton", MainFrame)
ToggleBtn.Text = "SISTEMA: OFF"
ToggleBtn.Size = UDim2.new(0.85, 0, 0.4, 0)
ToggleBtn.Position = UDim2.new(0.075, 0, 0.5, 0)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
ToggleBtn.TextColor3 = Color3.new(1, 1, 1)
ToggleBtn.Font = Enum.Font.GothamBold
Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(0, 6)

-- Configuración
local activo = false
local tamañoHitbox = 5 -- Tamaño de la caja de golpeo (Aumentado)
local rangoSeguro = 35 -- Distancia normal del juego

-- Función para expandir Hitbox
local function expandirHitbox(targetChar)
    local hrp = targetChar:FindFirstChild("HumanoidRoot
